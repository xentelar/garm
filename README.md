# Garm API Gateway

Based on the erlang platform for high performance and scalability on a 
single instance, **Garm** is a lightweight **API Gateway**, easily extended with adapters o config.

**Garm** runs over **erlang** 26,27,28,29

Build
-----

    $ rebar3 compile
